
var app = app || {};

(function($, _, Backbone){

app.StoryWidget = Backbone.Model.extend({
	
	initialize: function(){
		// the view rendering is done in the each loop of the collection
	}

});

}(jQuery, _, Backbone));
